# Changelog: FRONT-088 - Gantt Buffer Zone Adjustment

**Date:** 2026-02-21
**Task ID:** FRONT-088
**Status:** Verified

## Changes
- **Gantt Chart Buffer Zone**: Modified the "Buffer Zone" (grace period) in `frontend/src/components/project-gantt.tsx` to start one day *after* the task's `due_date`, providing a clearer visual distinction for the grace period.
- **Dependency**: Imported `addDays` from `date-fns` in `frontend/src/components/project-gantt.tsx` to support the date calculation.

## Verification
- Visual inspection of the Gantt chart confirms the buffer zone now starts one day after the due date.
- Frontend Docker build is successful (resolving previous `TS2304` error).
