# REAL-08 · Fix MCP assign_task tool

**Timestamp**: 17:15:00 Local Time  
**Author**: Ralph  

## Why
The `assign_task` tool in the MCP server was attempting to call a non-existent method `update_assignees` in `CRUDTask`, causing operations to fail when an AI agent tried to assign users to a task.

## What Changed
- `backend/app/crud/crud_task.py`: Implemented the `update_assignees` method to handle batch user assignments and trigger notifications for new assignees.

## Verification
**Command**: `cd backend; $env:PYTHONPATH="."; ..\.venv\Scripts\python.exe -m pytest test_mcp_standalone.py`  
**Result**: PASSED ✅  
**Output**:
```
collected 1 item
test_mcp_standalone.py .                                                                                 [100%]
1 passed in 0.45s
```

## Notes
- This fix ensures that the "Monolith Planner" MCP server is fully functional for task management tasks.
