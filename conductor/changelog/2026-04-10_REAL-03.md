# REAL-03 · Setup WebSocket Connection Manager

**Timestamp**: 14:50:00 Local Time  
**Author**: Ralph  

## Why
Real-time features like instant notification updates require a persistent connection between the server and the client. A WebSocket connection manager is needed to track active sessions by user ID.

## What Changed
- `backend/app/core/websockets.py`: Implemented the `ConnectionManager` class to handle WebSocket connections, disconnections, and targeted messaging (personal or broadcast).

## Verification
**Command**: `cd backend; $env:PYTHONPATH="."; ..\.venv\Scripts\python.exe -c "from app.core.websockets import ConnectionManager; m = ConnectionManager(); assert m"`  
**Result**: PASSED ✅  
**Output**:
```
(empty)
```

## Notes
- This manager will be used in the next task (REAL-04) to broadcast new notifications to active users.
- Authentication for WebSockets will need to be handled during the connection handshake in the API endpoint implementation (future task).
