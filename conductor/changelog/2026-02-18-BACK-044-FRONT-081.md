# Changelog - 2026-02-18 - BACK-044 & FRONT-081

## Task: BACK-044: Model: Advanced Dependency Types & FRONT-081: UI: Dependency Type Selection

### Changes:
- Backend:
    - Verified `Dependency` model support for `type` (FS, SS, FF, SF) and `lag_days`.
    - Confirmed `DependencyType` enum inclusion in `backend/app/core/enums.py`.
    - Ensured `DependencyCreate` schema supports new fields.
- Frontend:
    - Added `DependencyType` type to `frontend/src/types/index.ts`.
    - Overhauled `DependencyManager` component in `frontend/src/components/dependency-manager.tsx`.
    - Migrated from generic `blocked_by_ids` update to specific `/dependencies` POST/DELETE endpoints.
    - Implemented granular selection UI:
        - Two-step add flow (Select task -> Configure type/lag).
        - Dropdown for Dependency Type (FS, SS, FF, SF) with helpful descriptions.
        - Numeric input for `lag_days`.
    - Improved dependency list display to show type and lag (e.g., "Task A (FS +2d)").
- Verified frontend changes with `npx tsc --noEmit`.

### Implementation Details:
- Uses `Select` component for type selection.
- Integrated `Info` popover to explain dependency logic (Finish-to-Start, etc.).
- Robust delete using the dedicated endpoint `DELETE /tasks/{task_id}/dependencies/{predecessor_id}`.
