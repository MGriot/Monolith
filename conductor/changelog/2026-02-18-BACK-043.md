# Changelog - 2026-02-18 - BACK-043

## Task: BACK-043: Logic: Template Taxonomy Presets

### Changes:
- Updated `ProjectTemplate` model in `backend/app/models/template.py` to include `topics_preset` and `work_types_preset`.
- Updated `ProjectTemplate` schemas in `backend/app/schemas/template.py` to support metadata presets.
- Modified `ProjectCreate` schema in `backend/app/schemas/project.py` to include optional `template_id`.
- Implemented `_instantiate_template` in `CRUDProject` (`backend/app/crud/crud_project.py`) to handle:
    - Creation of project-scoped topics and work types from template presets.
    - Recursive creation of hierarchical tasks defined in the template.
    - Automatic mapping of template-scoped IDs to new database IDs for topics and types.
- Updated `CRUDTask.create` to support optional `owner_id`.
- Applied database migration via `backend/apply_template_presets.py`.
- Verified instantiation logic with `backend/test_template_instantiation_standalone.py`.

### Database Schema Updates:
- `project_templates`: Added `topics_preset` (JSONB) and `work_types_preset` (JSONB).
