# Changelog - 2026-02-18-BACK-041

## Task: BACK-041 - API: Support Independent Tasks

### Changes:
- **Models**: Updated `Task` model in `backend/app/models/task.py` to make `project_id` nullable.
- **Schemas**: Updated `TaskCreate`, `TaskInDBBase`, and `Task` schemas in `backend/app/schemas/task.py` to allow optional `project_id`.
- **CRUD**:
    - Modified `CRUDTask.create` to handle sorting logic when `project_id` is missing.
    - Updated `sync_project_from_tasks` to skip synchronization for project-less tasks.
    - Adjusted `notify_assignees` to generate correct links for independent tasks.
- **API**:
    - Updated `/tasks/assigned` and `/tasks/export` to include independent tasks via `outerjoin`.
    - Refined permission logic in `read_task`, `update_task`, `delete_task`, `upload_task_attachment`, `archive_task`, and `restore_task` to support project-less tasks based on ownership and assignment.
- **Database**: Applied manual migration to drop the `NOT NULL` constraint on `tasks.project_id`.

### Verification:
- Created and executed `backend/test_independent_tasks_standalone.py`.
- Successfully verified full lifecycle: Creation, Retrieval (with NULL project_id), and Removal.
