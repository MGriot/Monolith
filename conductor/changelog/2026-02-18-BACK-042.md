# Changelog - 2026-02-18 - BACK-042

## Task: BACK-042: Model: Scoped Taxonomy

### Changes:
- Refactored `Topic` and `WorkType` models in `backend/app/models/metadata.py` to support scoping.
- Added `project_id` and `task_id` columns to metadata tables with `CASCADE` deletion.
- Implemented partial unique indexes to enforce name uniqueness within specific scopes (Global, Project, Task).
- Updated `Project` and `Task` models with relationships to scoped metadata.
- Resolved `AmbiguousForeignKeysError` by explicitly defining `foreign_keys` in ORM relationships.
- Updated Pydantic schemas in `backend/app/schemas/metadata.py` to include scoping fields.
- Applied database migrations via `backend/apply_scoped_taxonomy.py` and `backend/fix_scoped_taxonomy_constraints.py`.
- Verified scoping logic with `backend/test_scoped_taxonomy_standalone.py`.

### Database Schema Updates:
- `topics`: Added `project_id`, `task_id`.
- `work_types`: Added `project_id`, `task_id`.
- Dropped old unique constraints on `name`.
- Added partial unique indexes:
    - `topics_global_unique_idx`
    - `topics_project_unique_idx`
    - `topics_task_unique_idx`
    - (Same for `work_types`)
