# Changelog - 2026-02-18 - FRONT-077

## Task: FRONT-077: UI: Native File Previews (Modal)

### Changes:
- Created `FilePreviewDialog` component using `@/components/ui/dialog`.
- Integrated `FilePreviewDialog` into `AttachmentManager`.
- Added native preview support for:
    - **Images:** (JPG, PNG, GIF, WEBP) rendered directly in the modal.
    - **PDFs:** Rendered using an `iframe` with basic browser controls.
- Added "Click to preview" hint and `Eye` icon for previewable attachments.
- Maintained "Download" and "External Link" options within the preview modal.
- Verified frontend changes with `npx tsc --noEmit`.

### Implementation Details:
- New file: `frontend/src/components/file-preview-dialog.tsx`.
- Updated `frontend/src/components/attachment-manager.tsx` to handle `previewUrl` state and `canPreview` logic.
