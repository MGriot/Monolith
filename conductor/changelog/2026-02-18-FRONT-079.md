# Changelog - 2026-02-18 - FRONT-079

## Task: FRONT-079: UI: Rich Dropdown Toggle Components

### Changes:
- Created a generic `RichDropdown` component in `frontend/src/components/ui/rich-dropdown.tsx`.
- Replaced chip-based multi-selection in `TaskForm` and `ProjectForm` with `RichDropdown`.
- Implemented features in `RichDropdown`:
    - Searchable items.
    - Support for single and multi-selection.
    - Custom item rendering (colors, icons).
    - Compact display of selected items using badges with removal icons.
    - Integrated loading state support.
- Verified frontend changes with `npx tsc --noEmit`.

### Implementation Details:
- Uses `@radix-ui/react-popover` for the dropdown overlay.
- Uses `lucide-react` for icons (`Search`, `X`, `Check`, `ChevronsUpDown`).
- Integrated into `frontend/src/components/task-form.tsx` for Assignees, Topics, and Work Types.
- Integrated into `frontend/src/components/project-form.tsx` for Project Members, Topics, and Work Types.
