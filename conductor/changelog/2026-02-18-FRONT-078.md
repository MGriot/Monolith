# Changelog - 2026-02-18 - FRONT-078

## Task: FRONT-078: UI: Scoped Taxonomy Management

### Changes:
- Created `ScopedTaxonomyManager` component in `frontend/src/components/scoped-taxonomy-manager.tsx`.
- Integrated `ScopedTaxonomyManager` into `ProjectDetailPage` within a new **Settings** tab.
- Added a full `ProjectForm` to the **Settings** tab for consolidated project management.
- Implemented CRUD functionality for project-scoped Topics and Work Types.
- Added visual feedback (badges, icons) for scoped metadata in the project header.
- Simplified `ProjectDetailPage` state and mutations for better maintainability.
- Verified frontend changes with `npx tsc --noEmit`.

### Implementation Details:
- The "Settings" tab now serves as the central hub for project configuration.
- Scoped metadata is fetched using existing API endpoints with `project_id` filter.
- Creation of scoped metadata automatically includes the `project_id` context.
