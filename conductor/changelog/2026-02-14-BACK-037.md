# Changelog - 2026-02-14 - BACK-037

## [BACK-037] - Enhance MCP Module: Admin Tools & Fixes

### Added
- `delete_user` tool to MCP server for administrative user removal.
- `update_task` tool to MCP server supporting updates to title, description, status, priority, and dates.
- `delete_task` tool to MCP server for task and subtask removal.
- `assign_task` tool to MCP server for managing task assignees.
- `assign_project_member` tool to MCP server for managing project membership.

### Fixed
- `create_subtask` tool logic: Now correctly resolves the parent project ID from the parent task, ensuring schema validation passes.
- Standardized MCP tool implementations to use `TaskCreate` where appropriate for consistency.

### Verified
- Executed `backend/test_mcp_standalone.py` covering all new tools and the fix for `create_subtask`.
- All tests passed in the project virtual environment.
