# NOTIF-01 · Define EmailService with SMTP configuration

**Timestamp**: 22:45:00 Local Time  
**Author**: Ralph  

## Why
The application required a functional email dispatcher to replace the previous print-based stub, enabling automated notifications as per the product requirements.

## What Changed
- `backend/app/core/config.py`: Added SMTP configuration fields (`SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD`, `EMAILS_FROM_EMAIL`) to the `Settings` class.
- `backend/app/core/notifications.py`: Implemented the `EmailService` class with SMTP delivery logic using `smtplib` and refactored existing notification functions to utilize this service.

## Verification
**Command**: `$env:PYTHONPATH="."; $env:DATABASE_URL="sqlite://"; $env:SECRET_KEY="test"; C:\Users\Admin\Documents\Coding\Monolith\.venv\Scripts\python.exe -c "from app.core.notifications import EmailService; s = EmailService(); assert hasattr(s, 'smtp_host')"`  
**Result**: PASSED ✅  
**Output**:
```
(empty)
```

## Notes
- `pydantic-settings` and other dependencies from `backend/requirements.txt` must be installed in the environment where the backend runs.
- The `send_email_notification` function currently calls the synchronous `send_email` method inline; in production, this should be offloaded to a background task runner.
