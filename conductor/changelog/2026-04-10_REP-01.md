# REP-01 · PDF Generation Service (xhtml2pdf)

**Timestamp**: 21:15:00 Local Time  
**Author**: Ralph  

## Why
To support professional reporting and customizable project status exports, the application requires a service to generate PDF documents from HTML templates.

## What Changed
- `backend/requirements.txt`: Added `xhtml2pdf` and `Jinja2` dependencies.
- `backend/app/core/pdf.py`: Implemented `PDFService` with methods for rendering Jinja2 templates to PDF and converting raw HTML to PDF.

## Verification
**Command**: `cd backend; $env:PYTHONPATH="."; $env:DATABASE_URL="sqlite://"; $env:SECRET_KEY="test"; ..\.venv\Scripts\python.exe -c "from app.core.pdf import PDFService; assert PDFService"`  
**Result**: PASSED ✅  
**Output**:
```
(empty)
```

## Notes
- The service defaults to looking for templates in `backend/app/templates`.
- Future tasks will define the HTML templates for project status reports.
