# Changelog - 2026-02-18 - FRONT-080

## Task: FRONT-080: UI: Two-Tier Assignee Selection with Preview

### Changes:
- Added `Team` interface to `frontend/src/types/index.ts`.
- Developed `AssigneeSelector` component in `frontend/src/components/assignee-selector.tsx`.
- Implemented hierarchical selection in `AssigneeSelector`:
    - "Teams" section: Allows selecting all members of a team at once.
    - "Individual Users" section: Standard user-by-user selection.
- Added Team Member Preview:
    - Integrated an "Info" icon per team that opens a `Popover` showing the list of members.
    - Shows a badge with the member count (+N).
- Integrated `AssigneeSelector` into:
    - `TaskForm` (for task assignees).
    - `ProjectForm` (for project members/access control).
- Verified changes with `npx tsc --noEmit`.

### Implementation Details:
- Fetches users from `/users/` and teams from `/teams/` using TanStack Query.
- Replaces the generic `RichDropdown` for user-related selections to provide a more tailored, high-signal experience for team-based assignment.
