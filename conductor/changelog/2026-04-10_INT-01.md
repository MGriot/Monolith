# INT-01 · LLM Service Integration (Anthropic/OpenAI)

**Timestamp**: 17:55:00 Local Time  
**Author**: Ralph  

## Why
To enable AI-powered features like automatic task decomposition, the application requires a centralized service to interact with LLM providers (OpenAI or Anthropic).

## What Changed
- `backend/requirements.txt`: Added `httpx` for asynchronous HTTP requests.
- `backend/app/core/config.py`: Added LLM configuration settings (`OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `LLM_PROVIDER`, `LLM_MODEL`).
- `backend/app/core/llm.py`: Implemented `LLMService` with methods for calling OpenAI and Anthropic APIs, including a high-level `decompose_task` method.

## Verification
**Command**: `cd backend; $env:PYTHONPATH="."; $env:DATABASE_URL="sqlite://"; $env:SECRET_KEY="test"; ..\.venv\Scripts\python.exe -c "from app.core.llm import LLMService; assert LLMService"`  
**Result**: PASSED ✅  
**Output**:
```
(empty)
```

## Notes
- The service defaults to OpenAI but can be switched via the `LLM_PROVIDER` environment variable.
- Task decomposition uses JSON mode (for OpenAI) to ensure structured output.
- Future tasks will expose this service via API endpoints and frontend buttons.
