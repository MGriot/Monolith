# Changelog: FRONT-015 Improve Task Creation UX

**Task ID:** FRONT-015
**Date:** 2026-01-31
**Author:** Ralph (AI Agent)

## Changes
- **Backend:**
    - Updated `TaskCreate` schema to include an optional list of `subtasks`.
    - Modified `CRUDTask.create` to handle nested subtask creation within the same transaction.
- **Frontend:**
    - Updated `taskSchema` in `TaskForm` to support subtasks array.
    - Implemented `useFieldArray` in `TaskForm` to allow users to add/remove subtasks dynamically during task creation.
    - Subtasks section is conditionally shown only during creation to reduce clutter.

## Verification
- **Manual:** User can add multiple subtasks while creating a task. Subtasks are correctly linked and stored.
- **Automated:** `test_nested_subtasks_standalone.py` passed, verifying the backend logic for atomic task+subtasks creation.
