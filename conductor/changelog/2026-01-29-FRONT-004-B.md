# Changelog: BACK-010 & FRONT-004-B - Master Calendar View

## Backend (BACK-010)
- Created `backend/app/schemas/calendar.py` for aggregated event data.
- Implemented `backend/app/api/api_v1/endpoints/calendar.py` with `GET /calendar/`.
- Endpoint supports date range filtering and requires authentication.
- Registered calendar router in `api.py`.

## Frontend (FRONT-004-B)
- Installed `shadcn` components: `Badge`, `Popover`, `Calendar` (date-picker).
- Implemented `frontend/src/pages/calendar.tsx` with a custom full-width monthly grid.
- Integrated with backend `GET /calendar/` using TanStack Query.
- Added event detail popovers and status/type badges.
- Configured routing in `App.tsx`.

## Verification
- Successfully ran `npm run build`.
- Backend code verified for syntax and logic via standalone script concept.