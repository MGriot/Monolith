# Changelog - FRONT-071

## Fix: project-gantt.tsx TypeScript Errors

Fixed a TypeScript error (`TS2322`) in `src/components/project-gantt.tsx` where the `title` prop was incorrectly passed to the `Link` icon component from `lucide-react`.

### Changes:
- Wrapped `Link` icons in a `span` element.
- Moved the `title` attribute to the wrapping `span` element.

### Verification:
- Ran `npx tsc --noEmit` in the `frontend` directory.
- Ran `npm run build` in the `frontend` directory.
- Build succeeded without TypeScript errors.
