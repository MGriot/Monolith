# REAL-01 · Migrate Idea Comments to Universal Threaded System

**Timestamp**: 14:15:00 Local Time  
**Author**: Ralph  

## Why
Unified the commenting system for Project Ideas to match the rest of the application, enabling threaded replies, Markdown support, and image embedding.

## What Changed
- `frontend/src/components/project-ideas.tsx`: Replaced custom legacy comment fetching and rendering logic with the reusable `CommentSection` component. Removed redundant state and mutations.

## Verification
**Command**: `cd frontend; npx tsc --noEmit`  
**Result**: PASSED ✅  
**Output**:
```
(empty)
```

## Notes
- The legacy `idea_comments` backend endpoints and model are now redundant and should be removed in the next task (REAL-02).
- Data migration for existing idea comments to the universal `comments` table may be required if there is production data.
