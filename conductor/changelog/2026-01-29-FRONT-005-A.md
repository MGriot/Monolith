# Changelog: FRONT-005-A - Project Dashboard Layout & Stats API

## Changes
- Created `frontend/src/pages/project-detail.tsx` with a tabbed interface.
- Implemented project header with description and progress bar.
- Added backend `GET /projects/{id}/statistics` in `projects.py` to aggregate activity by date.
- Registered `/projects/:id` route in `App.tsx`.
- Installed `@dnd-kit` and `react-calendar-heatmap` libraries.

## Verification
- Successfully ran `npm run build`.
- Page successfully fetches and displays basic project info and progress.