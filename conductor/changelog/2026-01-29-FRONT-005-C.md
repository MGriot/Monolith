# Changelog: FRONT-005-C - Project Gantt & Heatmap

## Changes
- Created `frontend/src/components/project-gantt.tsx` for task-level timeline visualization.
- Created `frontend/src/components/project-heatmap.tsx` using `react-calendar-heatmap`.
- Integrated components into `ProjectDetailPage`.
- Added `react-tooltip` for heatmap interactivity.
- Fixed TypeScript typing issues with `react-calendar-heatmap` definitions.

## Verification
- Successfully ran `npm run build`.
- Gantt chart correctly handles empty states and dynamic date windows.
- Heatmap correctly maps activity counts to color scales.