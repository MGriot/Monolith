# Changelog - 2026-01-30-FRONT-008

## Task: FRONT-008 - Notification UI (Bell & Dropdown)

### Changes
- Created `frontend/src/components/notification-popover.tsx` to handle user notifications.
- Integrated `NotificationPopover` into `frontend/src/components/layout.tsx`.
- Implemented "Mark as Read" and "Mark all as read" logic using backend API.
- Added automatic polling (60s) for new notifications.
- Styled notifications with icons and relative time (date-fns).
- Verified build stability with `npm run build`.

### Verification Results
- `npm run build`: Success.
- Visual inspection: Notification bell with unread badge and dropdown list is functional.