# Changelog: BACK-028 - Team Metadata Schema

**Date:** 2026-02-08
**Task ID:** BACK-028
**Status:** Verified

## Changes
- Updated `backend/app/schemas/team.py`:
    - Added `owner_id` to `TeamBase`.
    - Added `owner` relationship (User schema) to `Team` response model.

## Verification
- Created `backend/test_team_schemas.py`.
- Verified that `TeamCreate` accepts `owner_id`.
- Verified that `Team` response correctly hydrates the `owner` object.
