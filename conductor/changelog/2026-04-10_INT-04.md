# INT-04 · Webhook Integration Service

**Timestamp**: 19:10:00 Local Time  
**Author**: Ralph  

## Why
Users need to receive notifications in their existing communication tools like Slack and Discord. A generic webhook integration service allows the Monolith Planner to push updates to external platforms.

## What Changed
- `backend/app/core/webhooks.py`: Implemented `WebhookService` with dedicated methods for Slack and Discord webhook delivery, as well as a generic JSON payload handler.

## Verification
**Command**: `cd backend; $env:PYTHONPATH="."; ..\.venv\Scripts\python.exe -c "from app.core.webhooks import WebhookService; assert WebhookService"`  
**Result**: PASSED ✅  
**Output**:
```
(empty)
```

## Notes
- The service uses `httpx` for asynchronous requests to ensure it doesn't block the main event loop.
- It includes error handling and logging for failed delivery attempts.
