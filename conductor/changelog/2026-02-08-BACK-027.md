# Changelog: BACK-027 - Team Ownership Model

**Date:** 2026-02-08
**Task ID:** BACK-027
**Status:** Verified

## Changes
- Modified `backend/app/models/team.py`:
    - Added `owner_id` column linked to `users.id`.
    - Added `owner` relationship.
- Updated `backend/mcp_sync_db.py` to include `ALTER TABLE teams ADD COLUMN owner_id UUID REFERENCES users(id)`.
- Executed schema sync in Docker.

## Verification
- Checked database schema using `psql`:
    - `owner_id` column exists in `teams` table.
    - Foreign key constraint `teams_owner_id_fkey` is active.
