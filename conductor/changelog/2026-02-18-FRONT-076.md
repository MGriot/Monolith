# Changelog - 2026-02-18 - FRONT-076

## Task: FRONT-076: UI: Create Independent Task

### Changes:
- Added "New Independent Task" button to the **My Tasks** page.
- Implemented a Dialog with `TaskForm` to handle independent task creation (where `project_id` is null).
- Updated list and kanban views on the **My Tasks** page to correctly display "Independent" for tasks without a project.
- Fixed navigation logic in the **My Tasks** page to prevent navigation to a non-existent project page for independent tasks.
- Verified frontend changes with `npx tsc --noEmit`.

### Implementation Details:
- Updated `frontend/src/pages/my-tasks.tsx` to include `createTaskMutation` and state management for the creation dialog.
- Adjusted `activeTasks` filtering to support null projects.
- Added `Folder` icon and fallback text "Independent" in the task list.
